function rootReducer(state, action) {
  switch(action.type) {
    case "addToCart": {
      return;
    }
    case "removeFromCart": {
      return;
    }
    default:
      return;
  }
}

export default rootReducer;